{
  "name": "contentflow",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:web": "node scripts/use-source-index.mjs && npm --workspace @contentflow/web run dev",
    "dev:api": "npm --workspace @contentflow/api run dev",
    "build": "node scripts/use-source-index.mjs && npm --workspace @contentflow/web run build && node scripts/sync-pages.mjs && npm --workspace @contentflow/api run build",
    "build:web": "node scripts/use-source-index.mjs && npm --workspace @contentflow/web run build && node scripts/sync-pages.mjs",
    "build:api": "npm --workspace @contentflow/api run build",
    "test": "npm --workspace @contentflow/api test"
  },
  "dependencies": {
    "bull": "^4.16.5"
  },
  "devDependencies": {
    "docx": "^9.6.1",
    "typescript": "^5.9.3"
  }
}
